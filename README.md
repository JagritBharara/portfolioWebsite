# Portfolio Website

## Run Locally

```bash
npm install
npm run dev

Open:

http://localhost:5173
Docker
Build Image
docker build -t portfolio .
Run Container
docker run -p 8080:80 portfolio

Open:

http://localhost:8080
Docker Compose
Start
docker compose up --build
Stop
docker compose down